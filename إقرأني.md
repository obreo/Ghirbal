# غربال - متصفح الويب الآمن

## 🌐 نظرة عامة

غِربال هو متصفح ويب للهاتف المحمول يركز على توفير تجربة تصفح آمنة من خلال تصفية محتوى غير لائق، وحظر المواقع الإباحية، وتنفيذ قيود محتوى مختلفة. تم تصميم المتصفح لحماية المستخدمين من المحتوى غير المناسب مع الحفاظ على تجربة تصفح وظيفية.

## 🚀 الميزات

### ميزات السلامة الأساسية

- **حظر المواقع  ذات محتوى غير لائق**: يستخدم قائمة حظر معرفة مسبقًا مع خيارات للتخصيص.

يسمح للمستخدمين بإضافة domains مخصصة إلى قائمة الحظر من خلال ملفات محلية، إدخال يدوي، أو عناوين URL عن بعد مدمجة مع مرشحات EasyList و AdGuard و uBlock Origin. انظر `client/lib/Blocklist/blocklist.ts` و `client/lib/Blocklist/endpoints.ts` لمزيد من التفاصيل.

- **فرض بحث جوجل الآمن**: يجبر وضع بحث جوجل الآمن مع اكتشاف البحث المقيد

- **تصفية ريديت غير اللائقة**: وظيفة بحث مدمجة في مشاركات المجتمع، وإخفاء شريط البحث، وحظر قنوات ريديت ومشاركات غير لائقة.

- **وضع وسائل التواصل الاجتماعي المقيد**: يُسمح بإنستغرام لكل منشور/ريلز مباشر فقط، وإلا يتم حظره. يتم حظر مواقع وسائل التواصل الاجتماعي باستثناء فيسبوك إلى أوضاع آمنة حيثما ينطبق.

- **التحكم في محتوى يوتيوب**: يجبر البحث المقيد مع السماح اختياريًا بالتعليقات على مقاطع الفيديو، وإخفاء مقاطع الفيديو القصيرة والفيديوهات المقترحة

- **حظر الصور والفيديو**: يمنع اختياريًا تحميل الصور ومقاطع الفيديو على المواقع غير المدرجة في القائمة البيضاء (قابل للتهيئة)

- **التحكم في الوصول إلى جوجل**: يقيد الوصول إلى Google.com للسماح فقط بالتنقل من safesearchengine.com، والصور (اختياري)، وألسنة الفيديو القصيرة والفيديوهات المحظورة.

### الخصوصية والأمان

- **بوابة الأذونات**: تحكم دقيق في أذونات الموقع (الكاميرا، الميكروفون، الموقع، التخزين)

- **إدارة الكوكيز**: معالجة متقدمة للكوكيز والمسح

- **عزل البيانات**: إمكانات عزل بيانات الموقع ومسحها

- **تصفية المحتوى**: تصفية محتوى JavaScript للعناصر البصرية

### تجربة المستخدم

- **إدارة علامات التبويب**: دعم علامات التبويب المتعددة مع التبديل بين العلامات

- **نظام الإشارات المرجعية**: حفظ وتنظيم المواقع المفضلة

- **تتبع السجل**: سجل التصفح مع إمكانية البحث

- **واجهة متكيفة**: تصميم متجاوب يعمل على مختلف أحجام الشاشات

- **وضع الظلام/الضوء**: تبديل تلقائي للمظهر بناءً على إعدادات النظام

## 🛠️ التهيئة

### متغيرات البيئة (.env)

يستخدم التطبيق متغيرات البيئة للتهيئة. للإعداد:

1. أعد تسمية `BLOCKLIST_VARIABLES.env` إلى `.env`

2. قم بتهيئة المتغيرات التالية:

```env

# تمكين/تعطيل حجب الوسائط (الصور/مقاطع الفيديو)

ENABLE_MEDIA_BLOCKING='true'

# تمكين/تعطيل التحكم في الوصول إلى جوجل

ENABLE_GOOGLE_ACCESS_CONTROL='true'

# وضع يوتيوب المقيد (true = دائمًا مقيد، false = ديناميكي)

ENABLE_YOUTUBE_ALWAYS_RESTRICTED='false'

# تمكين/تعطيل حظر صور جوجل

ENABLE_GOOGLE_IMAGES_BLOCKING='true'

# تهيئة حظر ريديت

WILDCARD_REDDIT_BLOCKED_LIST=["nsfw","key2",...]

SUBREDDIT_PATTERNS_BLOCKED_LIST=["/r/nsfw","/r/subreddit2",...]

```
### تهيئة التطبيق

عدل `app.json` لتخصيص إعدادات التطبيق مثل الاسم، الأيقونة، شاشة البداية، وألوان المظهر.

### القائمة البيضاء للوسائط 

في حالة ضبط ENABLE_MEDIA_BLOCKING على true، للسماح بعرض الصور ومقاطع الفيديو على مواقع محددة، عدل `client/lib/media-whitelist.ts`:

```typescript

export const WHITELISTED_DOMAINS: string[] = [

  "safesearchengine.com",

  "wikipedia.org",

  "khanacademy.org",

  // أضف مجالاتك هنا

];

``` 

### قائمة حظر المواقع

لحظر مواقع إضافية، عدل `client/lib/Blocklist/blocklist.ts` مع المجالات التي تريد حظرها.

## 🏗️ بناء التطبيق

### المتطلبات

- Node.js (v18 أو أعلى)

- Expo CLI

- EAS CLI

- Git

- بيئة تطوير متوافقة (Windows مع WSL، Linux، أو Mac)

- Android Studio (لبناء Android محليًا)

- Java Development Kit (JDK 11 أو أعلى)

- Xcode (لبناء iOS محليًا على Mac)

- حساب Expo

### تعليمات الإعداد

1. **استنساخ المستودع:**

```bash
git clone <repository-url>
cd Ghirbal
```

2. **تثبيت التبعيات:**
```bash
npm install
```

3. **إعداد البيئة:**
- أعد تسمية `BLOCKLIST_VARIABLES.env` إلى `.env`
- حدّث قيم التهيئة حسب الحاجة

4. **للمستخدمين_windows باستخدام WSL:**
- إنشاء رابط رمزي من دليل Windows إلى دليل WSL:
```bash
# في محطة WSL
ln -s /mnt/c/Users/USERNAME/DIRECTORY_TO_Ghirbal ~/ghirbal
cd ~/ghirbal
```

5. **تهيئة Expo:**
- نسخ تهيئة التطبيق الاحتياطية من `backups/app.json` إلى الدليل الجذر كـ `app.json`
- سجّل في حساب Expo على [https://expo.dev](https://expo.dev)
- تسجيل الدخول إلى Expo CLI:

```bash
npx expo login
```

6. **بناء التطبيق باستخدام Expo السحابي:**
```bash
# تنظيف الإنشاء المسبق
npx expo prebuild --clean --platform android
# البناء للإنتاج
eas build -p android --profile production
# أو لنظام iOS
eas build -p ios --profile production
```

7. **بناء محلي بديل (لـ WSL/Linux/Mac):**
```bash
eas build -p android --profile production --locally
```

### التطوير

للتطوير، يمكنك تشغيل:

```bash

# بدء التشغيل مع Expo Go
npx expo start --lan -c

# أندرويد
npx expo run:android

# iOS
npx expo run:ios 

# الويب
npx expo start --web

```

## 🔐 الأذونات

⚠️ **إشعار مهم للأذونات**

يتضمن التطبيق أذونات تجريبية على مستوى النظام يجب أن تبقى **معطلة افتراضيًا**. قد تعمل بعض المواقع بشكل نشط حتى عند تعطيل الأذونات بسبب نظام بوابة الأذونات القائم على JavaScript للمتصفح.

### نظام الأذونات

- **الكاميرا/الميكروفون**: يجب أن تطلب المواقع إذن الوصول إلى الكاميرا/الميكروفون

- **الموقع**: يجب منح إذن الوصول إلى GPS صراحة لكل موقع

- **تخزين الملفات**: يجب تمكين الوصول إلى الملفات لكل موقع

- **الإشعارات**: يجب السماح بالإشعارات الفورية لكل موقع

- **الصور**: أذونات تحميل الصور لكل موقع

### الإعدادات الموصى بها

- اترك أذونات المستوى النظام معطلة افتراضيًا

- استخدم نظام الأذونات المدمج في المتصفح بدلاً من ذلك

- منح الأذونات حسب الموقع حسب الحاجة

- توفر بوابة JavaScript الحماية الكافية

## 📋 تهيئة قائمة الحظر

### قائمة حظر المواقع (`client/lib/Blocklist/blocklist.ts`)

- تحتوي على قائمة شاملة بالمجالات لحظرها

- يتم تحميلها وتطبيقها تلقائيًا على كل التصفح

- يمكن توسيعها بمجالات مخصصة

### قائمة حظر ريديت (تهيئة `.env`)

- `WILDCARD_REDDIT_BLOCKED_LIST`: أنماط حرف البدل لحзор ريديت

- `SUBREDDIT_PATTERNS_BLOCKED_LIST`: مسارات قنوات ريديت المحددة لحذورها

- يحذور أي اسم مجتمع يحتوي على الكلمات المحددة

## 📄 الترخيص

هذا المشروع مرخص بموجب ترخيص GNU General Public License v3.0 (GPL-3.0).

```
غِربال - متصفح الويب الآمن

حقوق الطبع والنشر (C) 2026 Obreo

  

هذا البرنامج عبارة عن برنامج مجاني: يمكنك إعادة توزيعه و/أو تعديله

بموجب شروط ترخيص GNU العام المنشور بواسطة

مؤسسة البرمجيات الحرة، إما الإصدار 3 من الترخيص، أو

(حسب اختيارك) أي إصدار لاحق.

  

يتم توزيع هذا البرنامج على أهل أن يكون مفيدًا،

ولكن بدون أي ضمانات؛ دون حتى الضمان الضمني ل

القابلية للتسويق أو الملاءمة لغرض معين. راجع

ترخيص GNU العام لمزيد من التفاصيل.

  

يجب أن تكون قد تلقيت نسخة من ترخيص GNU العام

جنباً إلى جنب مع هذا البرنامج. إذا لم يكن كذلك، راجع <https://www.gnu.org/licenses/>.

```

## 🤝 المساهمة

المساهمات مرحب بها! يرجى الشعور بالحرية في تقديم طلب سحب. للتعديلات الرئيسية، يرجى فتح مشكلة أولاً لمناقشة ما الذي ترغب في تغييره.

## 🐛 المشاكل

إذا واجهت أي مشاكل أو كانت لديك اقتراحات للتحسين، يرجى إنشاء مشكلة في المستودع.

- إذا لم يظهر التطبيق بشكل صحيح أيقونة أو شاشة البداية، فتأكد من أنك قد نسخت النسخة الاحتياطية من `app.json` من `backups/app.json` إلى الدليل الجذر. وقم باستبعاده من .gitignore.